<%= form_for @book, :url => book_path do |book| %>
	<%= book.text_field :book_name %>
	<%= book.hidden_field :author_id %>
	<%= book.submit "Edit Book" %>

<% end %>

<%= link_to 'Go Back ', author_path(@book.author_id) %>