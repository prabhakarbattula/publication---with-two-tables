<%= form_for @book, :url => update_book_path do |book| %>
	<%= book.text_field :book_name %>
	<%= book.hidden_field :author_id %>
	<%= book.submit "Edit Book" %>

<% end %>

<%= button_to 'Go Back ', show_author_path(@book.author_id), method: :get  %>