
<h4> Displaying search results for Authors </h4>

<head>
  <style>

      table, td, th
      {
          border:2px solid blue;
      }
      th
      {
          background-color: royalblue;
          color:white;
      }
      td, th
      {
          padding:5px;
      }
      tr:nth-child(even) {background-color: white}
      tr:nth-child(odd) {background-color: lightcyan}
  </style>
</head>

<body>
<table>
  <tr>
    <th> Name </th>
    <th>Sex </th>
    <th> Country </th>
    <th> ACTION </th>
    <th> ACTION </th>

  </tr>

  <% @authors.each do |author| %>
      <tr>
        <td><%= link_to author.author_name.upcase, author_path(id: author.id) %></td>
        <td><%= author.sex %></td>
        <td><%= author.country %></td>

        <td> <%= link_to "EDIT", edit_author_path(id: author.id), :class => 'btn btn-mini btn-warning' %> </td>
        <td> <%= link_to "DELETE", author_path(id: author.id), method: :delete, :class => 'btn btn-mini btn-danger' %> </td>
      </tr>
  <% end %>
</table>
</body>



