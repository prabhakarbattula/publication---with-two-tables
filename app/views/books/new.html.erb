<%= form_for @book, :url => books_path do |book| %>
	<%= book.text_field :book_name %>
	<%= book.hidden_field :author_id %>
	<%= book.submit "Create New Book" %>

<% end %>

<%= link_to 'Go Back ', author_path(params[:id]) %>