<%= form_for @author, :url => {:controller => 'authors', :action => 'update'}, method: 'get' do |author| %>
	<%= author.text_field :author_name %>
	<%= author.submit "Update Author" %>
<% end %>

<%= button_to "Go Back to Main Page", "/authors/index", method: :get %> 